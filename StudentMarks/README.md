# Student Marks Class

This program demonstrates the use of Classes and Objects in Python.

The Student class represents a student record system with the following features:

- Stores student name, roll number, and marks
- Uses getter and setter methods for controlled access to data
- Validates input values before updating attributes
- Prevents invalid data such as empty names, invalid roll numbers, and negative marks
